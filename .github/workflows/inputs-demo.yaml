name: Input Demo
on: 
  workflow_dispatch:
    inputs:
      enviroment:
        description: Choose enviroment to depoly
        required: true
        default: dev
jobs:
  inputs-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Depoly the App
        run: echo "Deploying the application to ${{inputs.enviroment }}"
